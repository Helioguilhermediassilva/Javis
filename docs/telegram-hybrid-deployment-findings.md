# Publicação do fluxo híbrido Telegram

## Vercel

Projeto: `javis` no time `NOWGO`.

Commit: `ddc95a7` — `feat(telegram): add hybrid bot linking flow`

Deployment observado em 19/08/2026: inicialmente `Building`, com alias público `https://javis-11ilpisw6-nowgo.vercel.app`.

O deployment anterior da autenticação (`1ef7248`) estava `Ready`; o novo build contém a dependência `qrcode.react`, o QR Code do chat público e a configuração automática do perfil do bot via Bot API.

## Repositório

`https://github.com/Helioguilhermediassilva/Javis`

Não registrar tokens Telegram, chaves Supabase ou segredos de criptografia neste arquivo.

## Validação pública

O alias `https://javis-11ilpisw6-nowgo.vercel.app/telegram-connect` abriu corretamente a tela de acesso do Xavier, pois a rota exige autenticação. O conteúdo público confirmou `XAVIER / ACCESS NODE` e **Inteligência Soberana com memória sob seu controle**. O painel QR Code requer uma sessão autenticada e não pode ser validado sem usar uma conta real ou um token Telegram fornecido pelo usuário.
