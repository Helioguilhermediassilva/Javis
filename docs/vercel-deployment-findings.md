# Deployment Vercel — projeto javis

Fonte: painel do projeto `https://vercel.com/nowgo/javis/deployments`

Em 19/08/2026, o commit `18be1af` (`feat(memory): add economical persistent Xavier memory`) atingiu `Ready` em `Production`, com alias `https://javis-8d2h56jwf-nowgo.vercel.app`.

A publicação inclui login multiusuário, memória resumida e econômica, controles de retenção e exclusão, a rota `/memory` e a integração unificada com Telegram. O deployment anterior do commit `6f09a44` permanece `Ready` com alias `https://javis-rjzk0in7f-nowgo.vercel.app`.

Validação pública concluída: o alias `javis-8d2h56jwf-nowgo.vercel.app` e o domínio oficial `https://jarvisnowgo.com/` exibiram a tela `XAVIER / ACCESS NODE`, com login, cadastro e aviso de memória controlada. O domínio oficial inicialmente mostrou `XAVIER / VALIDANDO SESSÃO` durante a hidratação e, em seguida, o mesmo conteúdo de acesso, comportamento esperado para a inicialização do provedor de sessão.
